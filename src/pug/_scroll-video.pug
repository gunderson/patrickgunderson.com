a(href=["",project.section,project.slug].join("/"))
    .main
        img(src=project.images[0])
        video(data-source=project.video[0], autoplay=true, loop=true, poster=project.images[0])
        .play-button-overlay
            .fa.fa-play

    .meta
        .title #{project.title}
            span.date #{project.date}