a(href=["",project.section,project.slug].join("/"))
    .main
        img(src=project.images[0])
        video(data-source=project.video[0], autoplay=true, loop=true)

    .meta
        .title #{project.title}
            span.date #{project.date}